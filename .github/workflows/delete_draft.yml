name: delete draft
on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/delete_draft.yml'
  workflow_dispatch:
name: Delete Draft Releases

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Delete drafts
        uses: hugo19941994/delete-draft-releases@v0.1.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
